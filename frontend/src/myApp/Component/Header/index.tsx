/*
*  zeplin design url
*/
import * as React from "react";
import * as ReactDOM from "react-dom";

type Props = {
  text: string,
  isEnabled?: boolean,
}

export const Header: React.FC<Props> = (props) => {
  return ()
}
